import component from './validation.component';
import { name as serviceName } from './validation.service';
import state from './validation.state';

export default {
  componentName: component.name,
  name: 'validation',
  serviceName,
  state,
  translationId: 'dedicatedCloud_servicePack_summary_stepper_header',
};
