import {
  COMPONENT_NAME,
  MODULE_NAME,
  STEP_NAME,
} from './constants';
import state from './state';

export default {
  componentName: COMPONENT_NAME,
  moduleName: MODULE_NAME,
  name: STEP_NAME,
  state,
  translationId: 'dedicatedCloud_servicePackActivation_selection_stepper_header',
};
